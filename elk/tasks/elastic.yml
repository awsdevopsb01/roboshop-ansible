- name: Copy elastic repo file
  ansible.builtin.template:
    src: elastic.repo
    dest: /etc/yum.repos.d

- name: Install Elastic Search
  ansible.builtin.yum:
    name: filebeat
    state: installed

